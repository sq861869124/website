import * as React from 'react';
import { CustomImg } from '~/common';
import './end-line.scss';

export const EndLine = () => {
  return (
    <div className="end-line">
      <CustomImg path="common" name="wml" />
    </div>
  );
};